pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                //bat 'cd /D D:\\java\\jenkins-java\\src && java App && javac App.java'
                git 'https://github.com/zoom2x/test.git'
            }
        }
    }
}
